{% extends 'base.html.twig' %}

{% block title %}TypeOfPlace{% endblock %}

{% block body %}
    <h1>TypeOfPlace</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_place.id }}</td>
            </tr>
            <tr>
                <th>TypeName</th>
                <td>{{ type_of_place.typeName }}</td>
            </tr>
            <tr>
                <th>TypePrice</th>
                <td>{{ type_of_place.typePrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_of_place_index') }}">back to list</a>

    <a href="{{ path('app_type_of_place_edit', {'id': type_of_place.id}) }}">edit</a>

    {{ include('type_of_place/_delete_form.html.twig') }}
{% endblock %}
