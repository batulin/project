<form method="post" action="{{ path('app_type_of_place_delete', {'id': type_of_place.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_of_place.id) }}">
    <button class="btn">Delete</button>
</form>
